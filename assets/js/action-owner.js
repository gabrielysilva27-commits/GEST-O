import { persistOperationalData, syncOperationalData } from "./shared-api.js?v=20260904-05";
const KEY="lead-gestao-db-v2";
function database(){const value=JSON.parse(localStorage.getItem(KEY)||"null");if(!value||!Array.isArray(value.actionPlans))throw new Error("Não foi possível acessar as ações.");return value;}
const norm=value=>String(value||"").normalize("NFD").replace(/[\u0300-\u036f]/g,"").toLowerCase().replace(/[^a-z0-9]+/g," ").trim();
export function canManageRequestedAction(action,user){if(!action||!user)return false;const id=Number(user.id);if(Number(action.createdBy)===id||Number(action.requesterId)===id)return true;const identities=[user.name,user.username,user.title].map(norm).filter(Boolean),requester=norm(action.requesterName);return Boolean(requester&&identities.includes(requester));}
function requestedAction(db,user,id){const action=db.actionPlans.find(item=>Number(item.id)===Number(id));if(!canManageRequestedAction(action,user))throw new Error("Somente o solicitante pode alterar ou excluir esta ação.");return action;}
export async function updateOwnedAction(user,id,values={}){await syncOperationalData();const db=database(),action=requestedAction(db,user,id),objective=String(values.objective||"").trim();if(!objective)throw new Error("Informe o plano de ação.");action.objective=objective;action.priority=["low","medium","high","critical"].includes(values.priority)?values.priority:action.priority;action.dueDate=values.dueDate||action.dueDate;action.updatedAt=new Date().toISOString();localStorage.setItem(KEY,JSON.stringify(db));await persistOperationalData();}
export async function deleteOwnedAction(user,id){await syncOperationalData();const db=database(),action=requestedAction(db,user,id);db.actionPlans=db.actionPlans.filter(item=>Number(item.id)!==Number(action.id));localStorage.setItem(KEY,JSON.stringify(db));await persistOperationalData();}