# LEAD Gestão

Aplicação operacional publicada via Cloudflare Workers.

> Este arquivo permanece apenas como referência do projeto. As funcionalidades do site estão nos módulos em `assets/js`, `assets/css`, `worker` e `recovered`.
